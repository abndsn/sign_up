<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>


      	<%= image_tag("http://localhost/travaux/gravs/" + "#{@user.image}", alt: @user.name, class: "gravatar") %>
       
        <%= @user.name %>
      </h1>
      <span><%= @user.email %></span>
    </section>
  </aside>
</div>